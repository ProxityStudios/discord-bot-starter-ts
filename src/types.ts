export enum CommandName {
  // General
  Hello = "hello",
  Help = "help",

  // Moderation
  AssignRole = "assignrole",
}

export enum CommandCategory {
  General = "General",
  Moderation = "Moderation",
}
